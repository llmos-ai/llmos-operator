/*
Copyright 2025 llmos.ai.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/
// Code generated by main. DO NOT EDIT.

package v1

import (
	"context"
	"sync"
	"time"

	v1 "github.com/llmos-ai/llmos-operator/pkg/apis/ml.llmos.ai/v1"
	"github.com/rancher/wrangler/v3/pkg/apply"
	"github.com/rancher/wrangler/v3/pkg/condition"
	"github.com/rancher/wrangler/v3/pkg/generic"
	"github.com/rancher/wrangler/v3/pkg/kv"
	"k8s.io/apimachinery/pkg/api/equality"
	"k8s.io/apimachinery/pkg/api/errors"
	"k8s.io/apimachinery/pkg/runtime"
	"k8s.io/apimachinery/pkg/runtime/schema"
)

// ModelVersionController interface for managing ModelVersion resources.
type ModelVersionController interface {
	generic.ControllerInterface[*v1.ModelVersion, *v1.ModelVersionList]
}

// ModelVersionClient interface for managing ModelVersion resources in Kubernetes.
type ModelVersionClient interface {
	generic.ClientInterface[*v1.ModelVersion, *v1.ModelVersionList]
}

// ModelVersionCache interface for retrieving ModelVersion resources in memory.
type ModelVersionCache interface {
	generic.CacheInterface[*v1.ModelVersion]
}

// ModelVersionStatusHandler is executed for every added or modified ModelVersion. Should return the new status to be updated
type ModelVersionStatusHandler func(obj *v1.ModelVersion, status v1.ModelVersionStatus) (v1.ModelVersionStatus, error)

// ModelVersionGeneratingHandler is the top-level handler that is executed for every ModelVersion event. It extends ModelVersionStatusHandler by a returning a slice of child objects to be passed to apply.Apply
type ModelVersionGeneratingHandler func(obj *v1.ModelVersion, status v1.ModelVersionStatus) ([]runtime.Object, v1.ModelVersionStatus, error)

// RegisterModelVersionStatusHandler configures a ModelVersionController to execute a ModelVersionStatusHandler for every events observed.
// If a non-empty condition is provided, it will be updated in the status conditions for every handler execution
func RegisterModelVersionStatusHandler(ctx context.Context, controller ModelVersionController, condition condition.Cond, name string, handler ModelVersionStatusHandler) {
	statusHandler := &modelVersionStatusHandler{
		client:    controller,
		condition: condition,
		handler:   handler,
	}
	controller.AddGenericHandler(ctx, name, generic.FromObjectHandlerToHandler(statusHandler.sync))
}

// RegisterModelVersionGeneratingHandler configures a ModelVersionController to execute a ModelVersionGeneratingHandler for every events observed, passing the returned objects to the provided apply.Apply.
// If a non-empty condition is provided, it will be updated in the status conditions for every handler execution
func RegisterModelVersionGeneratingHandler(ctx context.Context, controller ModelVersionController, apply apply.Apply,
	condition condition.Cond, name string, handler ModelVersionGeneratingHandler, opts *generic.GeneratingHandlerOptions) {
	statusHandler := &modelVersionGeneratingHandler{
		ModelVersionGeneratingHandler: handler,
		apply:                         apply,
		name:                          name,
		gvk:                           controller.GroupVersionKind(),
	}
	if opts != nil {
		statusHandler.opts = *opts
	}
	controller.OnChange(ctx, name, statusHandler.Remove)
	RegisterModelVersionStatusHandler(ctx, controller, condition, name, statusHandler.Handle)
}

type modelVersionStatusHandler struct {
	client    ModelVersionClient
	condition condition.Cond
	handler   ModelVersionStatusHandler
}

// sync is executed on every resource addition or modification. Executes the configured handlers and sends the updated status to the Kubernetes API
func (a *modelVersionStatusHandler) sync(key string, obj *v1.ModelVersion) (*v1.ModelVersion, error) {
	if obj == nil {
		return obj, nil
	}

	origStatus := obj.Status.DeepCopy()
	obj = obj.DeepCopy()
	newStatus, err := a.handler(obj, obj.Status)
	if err != nil {
		// Revert to old status on error
		newStatus = *origStatus.DeepCopy()
	}

	if a.condition != "" {
		if errors.IsConflict(err) {
			a.condition.SetError(&newStatus, "", nil)
		} else {
			a.condition.SetError(&newStatus, "", err)
		}
	}
	if !equality.Semantic.DeepEqual(origStatus, &newStatus) {
		if a.condition != "" {
			// Since status has changed, update the lastUpdatedTime
			a.condition.LastUpdated(&newStatus, time.Now().UTC().Format(time.RFC3339))
		}

		var newErr error
		obj.Status = newStatus
		newObj, newErr := a.client.UpdateStatus(obj)
		if err == nil {
			err = newErr
		}
		if newErr == nil {
			obj = newObj
		}
	}
	return obj, err
}

type modelVersionGeneratingHandler struct {
	ModelVersionGeneratingHandler
	apply apply.Apply
	opts  generic.GeneratingHandlerOptions
	gvk   schema.GroupVersionKind
	name  string
	seen  sync.Map
}

// Remove handles the observed deletion of a resource, cascade deleting every associated resource previously applied
func (a *modelVersionGeneratingHandler) Remove(key string, obj *v1.ModelVersion) (*v1.ModelVersion, error) {
	if obj != nil {
		return obj, nil
	}

	obj = &v1.ModelVersion{}
	obj.Namespace, obj.Name = kv.RSplit(key, "/")
	obj.SetGroupVersionKind(a.gvk)

	if a.opts.UniqueApplyForResourceVersion {
		a.seen.Delete(key)
	}

	return nil, generic.ConfigureApplyForObject(a.apply, obj, &a.opts).
		WithOwner(obj).
		WithSetID(a.name).
		ApplyObjects()
}

// Handle executes the configured ModelVersionGeneratingHandler and pass the resulting objects to apply.Apply, finally returning the new status of the resource
func (a *modelVersionGeneratingHandler) Handle(obj *v1.ModelVersion, status v1.ModelVersionStatus) (v1.ModelVersionStatus, error) {
	if !obj.DeletionTimestamp.IsZero() {
		return status, nil
	}

	objs, newStatus, err := a.ModelVersionGeneratingHandler(obj, status)
	if err != nil {
		return newStatus, err
	}
	if !a.isNewResourceVersion(obj) {
		return newStatus, nil
	}

	err = generic.ConfigureApplyForObject(a.apply, obj, &a.opts).
		WithOwner(obj).
		WithSetID(a.name).
		ApplyObjects(objs...)
	if err != nil {
		return newStatus, err
	}
	a.storeResourceVersion(obj)
	return newStatus, nil
}

// isNewResourceVersion detects if a specific resource version was already successfully processed.
// Only used if UniqueApplyForResourceVersion is set in generic.GeneratingHandlerOptions
func (a *modelVersionGeneratingHandler) isNewResourceVersion(obj *v1.ModelVersion) bool {
	if !a.opts.UniqueApplyForResourceVersion {
		return true
	}

	// Apply once per resource version
	key := obj.Namespace + "/" + obj.Name
	previous, ok := a.seen.Load(key)
	return !ok || previous != obj.ResourceVersion
}

// storeResourceVersion keeps track of the latest resource version of an object for which Apply was executed
// Only used if UniqueApplyForResourceVersion is set in generic.GeneratingHandlerOptions
func (a *modelVersionGeneratingHandler) storeResourceVersion(obj *v1.ModelVersion) {
	if !a.opts.UniqueApplyForResourceVersion {
		return
	}

	key := obj.Namespace + "/" + obj.Name
	a.seen.Store(key, obj.ResourceVersion)
}
